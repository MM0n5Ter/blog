{
  "name": "mmonster-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --local --watch",
    "serve": "node build.js --local && npx -y http-server dist -p 3000 -c-1",
    "sync": "node sync.js",
    "sync:build": "node sync.js && node build.js"
  },
  "dependencies": {
    "@notionhq/client": "^5.17.0",
    "dotenv": "^17.4.1",
    "gray-matter": "^4.0.3",
    "highlight.js": "^11.9.0",
    "marked": "^12.0.0",
    "marked-highlight": "^2.1.0",
    "notion-to-md": "^3.1.9"
  }
}
